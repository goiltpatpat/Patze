# Patze Platform Cordis Patch Overlay
# Configures skill discovery and inserts Patze core + Jev System 1 plugins

- id: skill-filesystem
  name: '@deepseek-ai/dsh-skill-filesystem'
  disabled: false
  config:
    watch: false
    customSkillDirs:
      - ../.agents/skills

- id: ui-sidebar-browser
  disabled: false

- insert:
    - id: patze-core
      name: ../src/index.js
